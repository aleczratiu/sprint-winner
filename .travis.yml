language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm run build

deploy:
  provider: s3
  access_key_id: $AWS_KEY
  secret_acess_key: $AWS_SECRET
  bucket: 'www.sprintman.cf.s3-website.eu-central-1.amazonaws.com'
  skip_cleanup: true
  local_dir: public
